---
// Legacy /blog/category/ → /reviews/ redirect stub (FEAT-blog-to-review-migration).
// The category index is removed (redundant with the /reviews browser); redirect so
// any old inbound link never 404s.
import RedirectStub from '@components/redirect-stub.astro'

export const prerender = true
---
<RedirectStub to="/reviews/" />
