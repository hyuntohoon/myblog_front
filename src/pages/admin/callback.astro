---
/* src/pages/admin/callback.astro */
---

<!doctype html>
<html lang="ko">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>로그인 처리중…</title>
		<meta name="robots" content="noindex" />
	</head>
	<body>
		<main class="auth-callback" role="status" aria-live="polite">
			<div class="spinner" aria-hidden="true"></div>
			<p id="msg" class="caption">로그인 처리 중…</p>
			<button id="retry" class="retry hidden" type="button">다시 로그인</button>
		</main>

		<!-- ✅ 호이스티드 스크립트 -->
		<script src="../../scripts/callback.client.ts"></script>
	</body>
</html>

<style is:global>
	/* Self-contained styles: the callback page renders before the site theme provider, so colors come from prefers-color-scheme rather than CSS vars. */
	:root {
		color-scheme: light dark;
	}

	html,
	body {
		margin: 0;
		min-height: 100vh;
	}

	.auth-callback {
		display: flex;
		min-height: 100vh;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 1.25rem;
		padding: 1.5rem;
		background: #ffffff;
		color: #1a1a1a;
		font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
	}

	.spinner {
		width: 38px;
		height: 38px;
		border: 3px solid rgba(0, 0, 0, 0.12);
		border-top-color: #1a1a1a;
		border-radius: 50%;
		animation: auth-spin 0.8s linear infinite;
	}

	.caption {
		margin: 0;
		font-size: 0.95rem;
		letter-spacing: 0.01em;
		opacity: 0.75;
	}

	.retry {
		padding: 0.55rem 1.1rem;
		border: 1px solid currentColor;
		border-radius: 8px;
		background: transparent;
		color: inherit;
		font-size: 0.9rem;
		cursor: pointer;
	}

	.retry:hover {
		opacity: 0.8;
	}

	.hidden {
		display: none;
	}

	@keyframes auth-spin {
		to {
			transform: rotate(360deg);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.spinner {
			animation-duration: 2s;
		}
	}

	@media (prefers-color-scheme: dark) {
		.auth-callback {
			background: #0f0f0f;
			color: #f2f2f2;
		}

		.spinner {
			border-color: rgba(255, 255, 255, 0.18);
			border-top-color: #f2f2f2;
		}
	}
</style>
